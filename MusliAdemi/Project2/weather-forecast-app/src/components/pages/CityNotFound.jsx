import React from 'react'

const CityNotFound = (props) => {
    console.log(props);
    return (
        <div>
            CityNotFound component
        </div>
    );
}

export default CityNotFound;